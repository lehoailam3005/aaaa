const request = require('request');const fs = require('fs')module.exports.config = {  name: "quaytay",  version: "1.0.0",  hasPermssion: 0,  credits: " Le Lam ",  description: " Thứ bạn cần hơn cả một nụ cười ! ",  commandCategory: "Hình ảnh",                                                                                                                                                                                                       usages: "địt [tag người bạn cần địt]",  cooldowns: 5,};module.exports.run = async ({ api, event }) => {  const axios = require('axios');  const request = require('request');  const fs = require("fs");  axios.get('https://image-random-api-1.vitnamngynaynam.repl.co/rushia?apikey=0bk3s6IAyq').then(res => {  let ext = res.data.data.substring(res.data.data.lastIndexOf(".") + 1);  let callback = function () {          api.sendMessage({            body:  "⚜ 1k flow fb chỉ 40k :3 Liên Hệ : www.facebook.com/lehoailam3005 ⚜",            attachment: fs.createReadStream(__dirname + `/cache/trinh.${ext}`)          }, event.threadID, () => fs.unlinkSync(__dirname + `/cache/trinh.${ext}`), event.messageID);        };        request(res.data.data).pipe(fs.createWriteStream(__dirname + `/cache/trinh.${ext}`)).on("close", callback);      })}